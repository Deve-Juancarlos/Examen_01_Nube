version: "1"

databases:
  - name: db-personas
    databaseName: db_personas
    user: admin_personas
    plan: free

services:
  - type: web
    name: web-personas
    runtime: docker
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: db-personas
          property: connectionString
